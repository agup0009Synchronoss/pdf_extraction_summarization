requests>=2.31
pikepdf>=8.7
pillow>=10.2
transformers>=4.41
torch>=2.3            # or cpu-only build if desired
easyocr>=1.7          # optional OCR (fallback)
paddleocr>=2.7        # Primary OCR engine
paddlepaddle>=2.5     # PaddleOCR backend (CPU version; use paddlepaddle-gpu for GPU)
docling>=0.7          # the Docling extraction library
tqdm>=4.66            # used by transformers download progress bars
gradio>=4.26