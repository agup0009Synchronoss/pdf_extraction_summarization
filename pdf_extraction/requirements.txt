requests>=2.31
pikepdf>=8.7
pillow>=10.2
transformers>=4.41
torch>=2.3            # or cpu-only build if desired
easyocr>=1.7          # optional OCR
docling>=0.7          # the Docling extraction library
tqdm>=4.66            # used by transformers download progress bars
gradio>=4.26