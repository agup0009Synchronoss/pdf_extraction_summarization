# DOTS OCR Application Requirements
# ==================================

# Core dependencies
torch>=2.0.0
gradio>=4.0.0
requests>=2.31.0

# PDF rasterization and image handling (real DOTS extraction)
PyMuPDF>=1.23.0
Pillow>=10.0.0

# Local vLLM client (OpenAI-compatible API to local DOTS server)
openai>=1.0.0

# vLLM server (for launch.sh on Linux/jovyan; runs in same venv)
vllm
