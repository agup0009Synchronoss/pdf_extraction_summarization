# DOTS OCR Application Requirements
# ==================================

# Core dependencies
torch>=2.0.0
gradio>=4.0.0
requests>=2.31.0

# PDF rasterization and image handling
PyMuPDF>=1.23.0
Pillow>=10.0.0

# HuggingFace Transformers (in-process DOTS OCR inference)
transformers>=4.45.0
accelerate>=0.30.0
qwen-vl-utils>=0.0.8

# Optional: install flash-attn separately for faster GPU inference
# pip install flash-attn --no-build-isolation
# Then set: export DOTS_ATTN_IMPLEMENTATION=flash_attention_2
